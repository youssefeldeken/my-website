# Base URL for the backend API. In local dev, Vite proxies /api to
# http://localhost:5000 automatically (see vite.config.js), so this can be
# left as-is. Set it explicitly when deploying frontend and backend to
# different domains, e.g. https://api.yourdomain.com/api
VITE_API_URL=/api
