To show your photo immediately without touching any code or running the
backend, just drop your photo into this "public" folder and name it exactly:

    profile.jpg

The homepage hero will automatically pick it up. If you'd rather manage it
from the admin dashboard instead (Settings > Profile Photo), that overrides
this file once the backend is running and a photo has been uploaded there.
